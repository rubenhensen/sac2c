SET (CFLAGS "-Wall -g -std=c99")
ADD_DEFINITIONS (${CFLAGS})

ADD_SUBDIRECTORY (libsac2c)
ADD_SUBDIRECTORY (runtime)
ADD_SUBDIRECTORY (libsac)

IF (ENABLE_PHM)
  ADD_SUBDIRECTORY (libsacphm)
ENDIF ()
