#!/bin/sh

while [ $# -gt 0 ]
do
  case $1 in
      -I*) include="${include} $1"
          ;;
      -o*) output=$2; shift
          ;;
      -*)
          ;;
      *) infile="${infile} $1"
          ;;
  esac
  shift
done

gcc ${include} -E -P -o ${output}.sl ${infile}

slc -o ${output} ${output}.sl
