#!/bin/sh

#
# This is obviously not the real configure script. 
# That resides in ./setup.
#
# We use this script to move all configure related files
# into a dedicated subdirectory rather than flooding the
# project root directory.
#

cd setup
./configure $@
cp sac2crc ..

