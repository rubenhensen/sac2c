# This file implements basic/common functionality that is to be
# include in the variosu test scripts. This file should *not* be
# called directly, but be sourced instead.

function die () {
    echo "error: $*" 1>&2
    exit 1
}
