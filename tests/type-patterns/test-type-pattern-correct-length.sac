// Test whether type patterns of shape `type[n]' are supported
// and the value `n' is assigned correctly.
//
// SAC_TEST|include common.mk
// SAC_TEST|SAC2C_FLAGS += -noprelude
// SAC_TEST|CALL_SAC2C := $(SAC2C) $(SAC2C_FLAGS)
//
// SAC_TEST|all: <file-name-we>
// SAC_TEST|<tab>$(CHECK_RETURN_STATUS) $< 2

int foo (int[n] a, int[n] b)
{
    return n;
}

int main()
{
    a = [1, 2];
    b = [1, 2];
    res = foo (a, b);
    return res;
}
